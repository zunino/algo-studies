package main

import "fmt"

func binary_search(slice []int, elem int) int {
    si := 0
    ei := slice.
    return 1
}

func main() {
    values := []int {4, 9, 10, 16, 32, 40, 42, 64, 96, 100}
    idx := binary_search(values, 16)
    fmt.Println("idx 16:", idx)
}
